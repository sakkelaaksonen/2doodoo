    <nav class="sticky-navi" aria-label="Main navigation">
      <h1>Todos: <span class="navi-listname" id="navi-listname"></span></h1>
      <button class="menu-btn" id="menu-btn" aria-label="Open menu" aria-expanded="false" aria-controls="menu-panel">☰</button>
    </nav>
   
      <div class="menu-panel" id="menu-panel" role="region" aria-label="Menu">
        <section class="container" aria-labelledby="form-title">
          <!-- ListApp UI only (list creation, selector, etc) -->
          
          <div id="list-title-container"></div>
          
          <form id="new-list-form" autocomplete="off">
            <div class="input-group">
              <label for="list-name-input">Add a new TodoList</label>
              <div class="input-row">
                <input
                  id="list-name-input"
                  class="text-input"
                  type="text"
                  maxlength="60"
                  aria-describedby="list-name-error"
                  aria-invalid="false"
                  required
                  title="Only letters and numbers are allowed for the list name."
                  placeholder="Max 60 letters and numbers"
                  aria-label="List Name"
                  name="list-name"
                />
                <button type="submit">Add</button>
              </div>
              <div
                id="list-name-error"
                aria-live="assertive"
                style="color: #b00; min-height: 1.5em"
              ></div>
            </div>
            <!-- List selector for switching between todo lists -->
            <div class="input-group">
              <label for="todo-list-selector">Select Todo List</label>
              <select id="todo-list-selector" aria-label="Todo List Selector"></select>
            </div>
          </form>
        </section>
      </div>
